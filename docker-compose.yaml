version: '3.7'
services:
    teamchooser:
        image: abusyd1/teamchooser
        build: ./
    db:
        image: mysql
        command: --default-authentication-plugin=mysql_native_password
        restart: always
        environment:
            MYSQL_ROOT_PASSWORD: example